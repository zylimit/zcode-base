# progress

## Pinned（长期约束）

- 宿主 = ZCode；只用原生扩展点（AGENTS.md/.zcode/用户级 hooks），规范源 = zcode-guide 官方 skills。
- runtime 零依赖 Node ≥18（ADR-0003）；hooks 单 dispatcher（ADR-0001）；账本哈希链 fail-closed（ADR-0002）。
- security/safety/privacy 三性永不可豁免、永不可 Fast 跳过；已执行的 FAIL 不可被 fast 豁免；Fast Mode 不豁免安全护栏。
- 家族资产保留复用：本仓七仓经验精华见 docs/ARCHITECTURE.md 与各 rules 头部来源标注。

## Decisions（决策流水）

- 2026-08-13 选 Workspace 布局+安装器形态（弃插件包：marketplace 摩擦大、skill 优先级最低）——用户拍板。
- 2026-08-13 硬门禁+留痕为默认强度（弃观察模式起步）——用户拍板。
- 2026-08-13 子代理角色契约放 docs/ROLE-CONTRACTS.md 而非 manifest agents 字段——zcode 记录但不执行该字段，诚实声明优于假装执行。
- 2026-08-13 hook 输出契约用退出码（0/2）+ additionalContext JSON——规避严格 schema 未知字段风险。
- 2026-08-13 commands 用嵌套目录 zbase/<verb>.md → /zbase:verb——Windows 文件名不允许冒号。
- 2026-08-13 git 路径枚举用 -z NUL 分隔（-z 已是无转义原始输出，无需 quote-path 选项）。
- 2026-08-13 Stop 门自计数封顶 2（ZCode 原生上限 3，留 1 次余量防死循环）。
- 2026-08-21 hooks 注册迁用户级 ~/.zcode/cli/config.json（ADR-0006）——工作区 hooks 每会话弹审核且无批量同意，门禁实测长期离线；用户级无审核即生效，wrapper 自检放行非 zcode 项目；doctor 双通道校验，工作区模式不堵死——用户拍板「迁移吧」。
- 2026-09-01 dsh-base 深研吸收（Spec v1.1，REQ-13..31，DEV-PLAN Phase 6-8 三批）：privacy 入永不可豁免保护集、Fast 改贷款语义（reason 必填/8h 封顶/DEBT 阻断）、recap+invariants 恒定成本恢复、sync-check+git hooks 补三文件同步执法缝、嵌套 AGENTS.md 模块契约、review 全链引擎化（结构化分歧）、八属性六档、需求追溯；fleet 多仓层暂缓（OQ-3）——用户指令「学习 dsh-base 优化 zcode-base 做到最好」；研究依据 .zcode/state/research/dsh-base-mechanisms.md（422 行源锚点报告）。
- 2026-09-01 升级为 v2.0 大重构（Spec v2.0，REQ-32..34）：单目录封装 .zcode/（学 dsh .dsh/ 哲学，安装面=一个目录+根级种子）、三缝执法（ZCode hooks+git hooks+CI）、自我插桩超越层（检查有效性计数——dsh 报告自评「治理处方几乎全部未被度量」正是缺口）；放弃 .agents/ 跨工具通用路径（宿主即 ZCode，深绑优于泛化）；DEV-PLAN 重排 R1-R6 六批，R1 纯结构迁移先行——用户指令「不计成本、大规模重构、比 dsh 更好」。并行三路家族仓增量研究（cursor/codex/cc-base，产出 .zcode/state/research/*-delta.md）+ 联网输入（agents.md 标准、Martin Fowler SDD 对比、OpenAI 治理 Cookbook）。
- 2026-09-01 cursor-base 增量裁决（报告 .zcode/state/research/cursor-base-delta.md）：①fast mode 之争两案叠乘——保留 dsh 贷款四条件 + 叠加铁律「已执行的 FAIL 永不可 fast 豁免」（跳过未运行≠豁免已证缺陷），超越两家；②增量并入批次：R3=withStateLock 状态锁/redactSecrets 输出脱敏/task known_hashes 写前冲突检测（纪律 2 执法化）/quarantine 隔离/boundedHead·PASS 静默·空计划 BLOCKED 杂项；R4=账本轮转+anchor/retention 引用保护/FAIL-streak 根因重定向/feedback 引擎化 lint/install 三方合并 upgrade·uninstall+safeManagedPath/waiver Compensation·Approval/性能锚点测试+256MB maxBuffer；R5=adapters 八属性证据供给侧/runtimeValidityHours 时间窗/plan_sha256；R6=shell 语义解析器+管道级秘密外传检测；③立场吸收：「检查优先于常驻文本」为宪法体量标尺、progress 模板加 Not doing 段、教训库不随 install 旅行、config 形缺陷同类实例复发必搜（feedback 种子）；④service 监督不整体移植（摘 probeHealth 语义 + 重启风暴熔断教训）。
- 2026-09-01 codex/cc 增量裁决（codex-base-delta.md 473 行 / cc-base-delta.md 265 行）：①缺陷修复级必做（R3）——跨进程状态锁（两仓独立同证）、untracked 内容入指纹（WIP 期证据绑定盲区）、全局输出脱敏 13 类（receipt note 现原样落 token）；②最高价值（R4）——verification plan（该跑什么=impact 确定性函数+planHash）、evidence 三重完整性、executor 角色绑定（一行执法宪法第 4 条）、completion 完成门聚合、install 事务性三仓合流；③cc 会话闸门（R3）——three-file-sync 挂 Stop/recap-on-dirty/check-evolution 播报/按栈编译门；plan-lint/skill 触发式描述③④/test-routing 双向一致性/CoVe verificationQuestion（R4）；make-release 泄漏自验（R4）、adapters 表（R5）、design-brief/dfx 12 维六要素/evolution 第④层/修复熔断（R5）；④R6 旗舰=shell 语义分类器 v2 四仓融合（tokenizer+管道外传+融合参数+三档决策+match/notMatch 自测向量）；⑤明确不做——service 监督（三仓共识拒绝）、auto-push、SubagentStop 机制（ZCode 无此事件已查证）、sh/ps1 双写（Node 免疫）、path lease（缓）；⑥新 OQ-4/OQ-5；⑦关键教训——量化指标否决（specification overfitting）、审查承重墙是外部证据非对抗形式、双真相源拒斥。
- 2026-09-01 R1 结构重构完成（M4 里程碑）：脚手架本体全收 .zcode/（lib×19/skills×17/commands×16/feedback/rules/docs/harness/state 运行态 gitignored）；109 项变更 git rename 保历史；55+ 文件路径切换零残留；install 单目录化（隔离冒烟 96 文件）；manifest 99 文件零漂移。主 Agent 新鲜验收：npm test/doctor/selftest 0.21s/manifest/hook 全 exit 0；用户级 wrapper 8 处已切 .zcode/zbase.mjs（双环境冒烟过）。DEV-PLAN v2.0 定稿：Phase 7=13 Task 地基（R3）、Phase 8=11 Task 体系（R4）、Phase 9=6 Task 扩展（R5）、Phase 10=5 Task 超越+终验（R6），每 Task 标源仓锚点。
- 2026-09-01 F1 修法裁决（R3a review 实证 P1：fast 债务收口逃逸链——SKIPPED 落账后改一行代码，fastDebtReceipts 按 fresh 过滤致债务随指纹漂移蒸发，task finish 无痕通过；叠加 critical 档可被 fast skip 的档位语义缺口）：①DEBT 查询改任务/窗口维度——不按 fingerprint fresh 过滤，窗口内产生的 SKIPPED 债务持续到还清（risk scan 同理）；②critical/high 档 check 不可 fast skip——ENFORCE_LEVELS 判定前置于 fastSkipValid，blocking 档在 fast 窗口内未跑=BLOCKED 语义非 SKIPPED（与 rules/quality-attributes.md「可跳：medium/low 档」对齐）；③red-locks：先锁「跳过后改代码再 finish 必须阻断」+「critical 档不可 skip」两条失败测试再修。修复与 R3b 串行（同文件并发写冲突），R3b 验收后统一发修复单（F1-F5：另含 quarantine 仅 SyntaxError 隔离/E2BIG 响亮失败/untracked 预算+memoize/tests 入软执法）。
- 2026-09-02 账本预算与 evidence 钉两裁决：①memory.maxLedgerBytes 24000→33000（harness 覆写）——默认按 ~600B 条目标定，本仓 v2.0 流水单条 ~3KB 且 Decisions/Open Issues ~17KB 属不可归档合法体量；校准优于留永久红警（警报疲劳比无警报更糟），33000 档保留最新 3 条 Done，每 1-2 条新流水自然触发归档循环。②receipt evidence 钉 supersede（verifyLedger 同路径仅最新 seq 钉担责，与 latestReceipts 后到覆盖先到同哲学）——否决轮转处置（rotateKeep=1 会丢 71 条回执内容，anchor 只留链值不留内容）；复审确认 supersede 是「再证明」模型非漏洞：信任根本就是 append-only 链+本地写权限，洗白历史全量留链可审计（seq72→73 即合法实例）。

## Done（完成流水）

- 2026-09-02 CI 修复第 2 轮：清零 windows 剩余 15 红（15 红→7 根因；implementer 派单被用户中断但改动已完整落盘，code-reviewer 全量逐 hunk 复审 ACCEPT/PASS 收编）。根因与修法：①doctor.mjs 备份文件名 nowIso() 含冒号 Windows 非法（copyfile ENOENT 致 install 全失败，#28/#153/#161）→ 时间戳 : 与 . 替换为 -（保字典序）；harness.test.mjs 新增备份名 Windows 非法字符集回归锁（任意平台真跑）。②make-release.sh MINGW 分支 MSYS /tmp 路径直传原生 Windows python3（WinError 3，make-release 6 用例）→ bash→python3 全部传参点 cygpath -w 显式转换，cygpath 缺失 fail-visible 报错不发半包；r4d/r4fix make-release 用例平台分支（win32 期望 .zip + python3 zipfile 列名 helper，posix tar 零漂移）。③scan.mjs findings file 字段 path.relative win32 反斜杠直出（#90 八规则断言全空）→ 全量 rel() 归一（posix 恒等零回归）。④core.mjs git() 响亮抛补 EINVAL（Windows 参数溢出形态，#49）。⑤PF1-4 沙箱无 .gitattributes + runner autocrlf=true → CRLF 污染 19 红（#60）→ 沙箱复制仓根 .gitattributes + git 写操作统一 -c core.autocrlf=false。⑥POSIX 语义守卫：F2 chmod 000 win32 skip（理由记录 NTFS 无权限位语义）；r3b 钩子执行位 win32 改断言文件存在+hooksPath（保留真实行为面）。⑦#112 平台断言动态对立平台（恒 BLOCKED，语义增强为无条件断言）。顺手修：progress.md 历史 push protection 记录含 xoxb 字面片段致主树 make-release dry-run 泄漏自验误拦（git archive HEAD 语义命中 HEAD 版本）→ 改述避让，commit 后 dry-run 自愈（reviewer Not-verified 项：commit 后复验 exit 0）。npm test 204/204；manifest check 153 文件；catalog lint 187→188 路径。用户指令落 feedback：ci-is-investment（CI 红债当轮清零+同轮回归锁）。reviewer P3 记录：r4b:273 fixture 名 win-only 名实不符（win32 上实声明 linux）、make-release r'$VAR' 单引号嵌入预存注入面（Temp 路径现实不含单引号，可接受）、r4d artifactPath 依赖默认 TMPDIR 假设。

- 2026-09-02 修复 CI gate 工作流全红（bug-fixer 流程：tester red-locks → implementer×2 → code-reviewer 复审，全子代理链）：origin 两次 push 的 4 矩阵作业全失败，两类确定性根因。①windows 130 单测红——a) 8 个测试文件 `new URL(...).pathname` 取路径在 win32 前导斜杠串盘符（`D:\D:\...`，cpSync 全 ENOENT 级联：r4a/b/c/d、r3b、r4fix、r4fix2、mechanisms）；b) `core.mjs userConfigPath()` 用 os.homedir()，Windows 读 USERPROFILE 不读 HOME，注释承诺的 HOME 测试隔离失效且 install 把配置写进真实 `C:\Users\runneradmin\.zcode\`。②ubuntu dod exit 2——attributes 阻断步读 `.zcode/state` 本机回执（不随分支旅行）CI 必红，gate.yml 注释自认的接线缺口。修法三处：core.mjs 改 `process.env.HOME || os.homedir()`（全仓 os.homedir 运行时调用仅此一处）；8 文件统一 fileURLToPath（对齐 helpers.mjs 既有模式）；gate.yml dod 前新增 8 条 gate 命令自落回执步（matrix 9 检查中全部带 command 者；hook-deny-matrix 无 command 走人工不在列；runGate 恒 bash -c，windows runner 预装 Git Bash）。red-locks：tests/platform-ci.test.mjs 4 用例——PF1-1 homedir monkey-patch 模拟 Windows 语义锁 HOME 优先、PF1-2 静态扫描禁 pathname 反模式、PF1-3 gate.yml 契约（dod 前必有 gate 步）、PF1-4 机制锁（沙箱 install 冒烟→8 gate→dod exit 0 端到端）；修前 3 红 1 绿（HEAD 快照注入复现确证非恒真）→ 修后 4 绿。review 裁 FIX_REQUIRED 唯一 P2：r4b 8.4 `legacyEvidenceReceipts>=30` 硬锚在账本归档后开发机必红（skip 守卫只挡「无账本」不挡「已归档」，本会话跑 gate 落账后实测复现）——修为已归档容忍分支（归档文件存在且含回执痕迹时 legacy=0 放行；status===0 与 total>0 两断言无条件保留守原意图），r4b 16/16 绿。终验：npm test 204/204 零红零跳过；manifest check/catalog lint 绿（manifest 重生成吸收 core.mjs 哈希）；本机 gate 序列 7 条回执落账（harness-doctor 环境性 FAIL 留痕：本机 hooks 未注册，CI 有 install 冒烟步先行不受影响；同指纹 FAIL+PASS 并存仍 uncovered 属反证优先保守语义，commit 后指纹变更自然 stale）。附带发现记档：node:test 注入的 NODE_TEST_CONTEXT env 传播会使子进程 `node --test` 23ms 空转假绿——仓内现有测试无受害者（run-tests.mjs 为顶层 launcher；r6a 的 npm test 串是分类器输入），PF1-4 已剥除该变量，二期若新增嵌套 --test 用例须防。Windows 实机行为本地不可复现（WSL），最终验收=push 后 CI 矩阵 4 作业全绿。





- Older entries are in [progress.archive.md](progress.archive.md).

- 2026-09-01 Phase 10 R6b 落地（Task 10.2/10.3/10.4，implementer 子代理，与 R6a 共享树并行——hooks.mjs/classifier 零触碰）：①Task 10.2 自我插桩 effectiveness（REQ-34，cc N7）——quality.mjs 原 audit 节新增 effectiveness()（gate-audit 死闸审计升级：每规则 {deny,observe,allow}+other（非三态动作计数不丢弃）+lastTriggered；unexercised=阻断类事件（PreToolUse/PermissionRequest/Stop，hook exit 2 契约）上 deny===0 且非 pass-through（'ok' 放行留痕 deny===0 是设计；清单可经 harness.json effectiveness.passThroughRules 扩展防分类器改名锁死）；blindSpot 显式标注「零留痕规则不在账」不假装全知；出口统一 redactSecrets 与 logGate 入口对称）+ zbase effectiveness 子命令（说明性报告 exit 0 不阻断，usage 同步；audit.mjs shim export * 自动 re-export）。本仓真实 gate-log 冒烟：10 规则留痕/累计 deny 7/unexercised 0（rm-rf-root 2、stop-gate 2+exhausted 4、sudo/git-force-push/write-preflight 各 1——闸都说得出挡住过什么）。②Task 10.3 live 路由断言库（cc H 方法论移植，断言层零宿主依赖先行）——tests/routing/test-helpers.mjs（loadEvents 坏行 throw fail-visible；assertSkillInvoked 锁同一 tool_use 的 input.skill，skill 名只在别处文本出现给 cross-line-decoupled 诊断；assertNoPrematureAction 首个 Skill 前白名单外 tool_use=偷跑 FAIL、全程无 Skill 也 FAIL、白名单默认 Skill/TodoWrite/Read 可调；assertOrder/assertContains）+ fixtures 三件（good-run/premature-run/cross-line-decoupled，按 ZCode 工具调用 JSON 形态 {"type":"tool_use","name":"Skill","input":{"skill":"x"}}，OQ-5 字段名待实测校准已在断言库头注与 cases/README 标注；断言只依赖 type/name/input.skill 三字段压小校准面）+ selftest=tests/routing-selftest.test.mjs 顶层命名（launcher 只扫 tests/ 顶层 *.test.mjs 不递归，选移顶层不改 launcher，6 用例：good 全过/premature 拒并点名 Edit/cross-line 拒+fixture 原始文本确含 skill 名与 Skill 行的对抗非空断言/无 Skill 拒/坏行 throw）+ tests/routing/cases/README.md live 骨架（RUN_LIVE_ROUTING=1 默认 opt-out 防烧 token；环境探针：认证失败或零 Skill 事件→SKIP 全部带诊断≠PASS，cc 代理环境坑的现成答案；真 case 待 OQ-5 落地后按命名模板添加）。no-direct-code-guard 台账模式（cc A1 降级实现——.zcode/state 层做不了且 hooks.mjs 属 R6a 在途）：宪法纪律 3 句尾括注「（台账执法待 OQ-4 实测子代理触发域后接线）」+ 存量条目 main-agent-no-direct-coding occurrences 3→4（cc A1 硬拦实现与其子代理触发域歧义留档，台账执法先行不硬拦）。③Task 10.4 feedback 种子 15 条（三仓教训，每条一句话+来源仓+事故语境 2-3 行均自 .zcode/state/research/ 三报告提取非虚构；occurrences 按来源仓记录次数）：multi-repo-commit-isolation（codex 耦合脚本半成功烂局）/preserve-brand-assets（codex Logo 误删 git 历史恢复）/waiver-lifecycle-explicit（occ3：codex 3 条豁免 12 天后才显式关闭）/main-agent-verifies-key-sources（codex native-subagent-research）/metric-gaming-warning（cc 撤回量化成功指标，specification overfitting）/sibling-config-instances（occ2：codex hook timeout 6 处修 1+cursor 同型——修复动作面）/watching-hung-process-is-hope（cursor 等挂起进程是最贵的希望）/uptime-misleads（cursor 旧进程滞留误导，验收读创建时间戳/镜像 tag）/tag-check-remote-first（cc 冲突 tag）/hook-schema-instability（cc .tool_exit_code 永假，关联 OQ-2）/gate-earns-place（cc 闸要能说出它挡住过什么——effectiveness 已接线数据面）/read-deep-not-surface（occ2：cc 五步闸两起翻车存档）/single-truth-source-rejection（occ3：cc 三次同理由否决）/spec-overfitting-quantitative（cc 决策的回执/验收面应用，与 metric-gaming 交叉引用）/config-defects-recur（occ2：完成声明覆盖故障类别——判定口径面，与 sibling 互补不合并删并）；FEEDBACK-INDEX 更新（20 条目；occ≥3 毕业候选 7 条：存量 5+waiver-lifecycle+single-truth-source）。④联动——r4a feedback 条目数锚点 5→20（era 锚点防条目意外丢失）。终验：npm test 180/180（169 基线+11 新增零回归）；feedback lint exit 0（20 条目全合规）；scan-instructions 0 error；doctor ok exit 0（manifest 重生成后零漂移 152 文件比对）；selftest ok 152ms。并行呈报：manifest 重生成吸收了 R6a 在途 .zcode/lib/classifier.mjs 的当时态哈希（生成物无法排除外来文件；R6a 收尾重生成 manifest 即自愈，若其未重生成 doctor 将显式报 critical 漂移——fail-visible 非静默，回执 Not-verified 注明）。

- 2026-09-01 Phase 10 R6a 落地（Task 10.1 shell 语义分类器 v2——四仓融合旗舰，implementer 子代理，与 R6b 共享树并行——audit/quality.mjs 零触碰）：①新 lib/classifier.mjs（约 420 行零依赖纯函数）替换 hooks.mjs checkBashCommand 的正则黑名单直测——A. tokenizer（codex shellTokens 移植：引号状态机/$(...)反引号*? 标 dynamic/换行=;/>>||&& 合并/>|;& 分 kind；反斜杠不消费保 Windows 路径可识别，cursor 教训的反向取舍）；B. wrapper 剥壳（codex effectiveWords：env VAR=1/sudo -u root/nice -n 5/timeout -k 1 -s TERM 5 值旗标跳两格+时长跳格/command|exec|nohup|time|builtin 平剥——「timeout 5 git reset --hard 被分类为名为 5 的程序」历史缺陷类根除）；C. 嵌套递归（bash/sh/zsh/dash/ksh/busybox -c/-lc、pwsh -Command payload 递归再分类 depth≤3）；D. 危险语义 14 deny 规则（git-reset-hard/git-clean/git-force-push（--force-with-lease 天然放行——只有精确 --force/-f/--mirror 才拦）/rm-rf-root（rootishTarget：/ /* * ~ ~/ 盘符根 动态$HOME）/recursive-forced-deletion（rm -r+f 任意目标 codex 口径+del|rd /s /q+Remove-Item -Recurse -Force）/chmod-777/recursive-system-chmod/broad-kill（pkill|killall|kill -9|taskkill /F）/mkfs-dd-disk/machine-shutdown/fork-bomb（唯一文本级规则，锚定命令执行位置 ^ ; & | ` $( ——echo 炸弹字符串不误伤）/curl-pipe-shell（REMOTE_FETCHERS→|→SHELL_INTERPRETERS 跨段+嵌套双路）；旧 rule id 全保持（rm-rf-root/git-reset-hard/... gate-log 连续性）；E. 管道级秘密外传（cursor 两遍式+codex 融合参数）——sensitivePath（.env 非 example/sample/template+pem/key/p12/pfx/jks/keystore+id_rsa 族前缀+credentials/secrets.json/service-account.json/.netrc/.npmrc/.pypirc+.ssh/.aws/.azure/.gnupg/.kube/.docker 目录，反斜杠折叠跨平台）；融合参数提取（-d@.env/--data=@.env/file=@id_rsa/-T.env/--env-file=.env 四形态候选）；跨管道段跟踪（cat id_rsa | nc host port 任一段碰敏感+后续段∈EGRESS 16 命令→deny secret-egress；cat .env | curl -d @- pipedSecret）；dd if=<secret>→secret-read；reader 19/copier 6 名单；F. 三档决策——deny=exit 2（危险/外传/读取/复制，secret-read 旧 deny 档位保持）；ask=放行+gate-log observe(规则 id，喂 R6b effectiveness)+additionalContext 提醒（privilege-escalation（sudo/doas/runas 无危险内层）/data-upload（-d/--data*/-F/-T/--post-* 含=连写）/unverifiable-egress（外发+动态标记）/sensitive-touch（非 reader 命令碰敏感路径））；allow。项目附加正则面保持（harness.json risk.confirm dangerousCommands/secretReadPatterns 默认改 []，opt-in raw 串直测语义不变）。②规则自带向量——.zcode/harness/classifier-rules.json 19 规则 146 向量（每规则 match/notMatch+allow 清单；codex safety.rules 思想）+ zbase classifier lint 子命令（向量即规则契约：不一致=契约破坏 exit 1 非检查发现；路径按 projectRoot 解析与 hook 同语义）；lint 首跑即抓出 --post-data= 连写形态漏配（改 DATA_UPLOAD_FLAGS 为前缀+边界匹配）——自测闭环实证有效。③hooks.mjs 消费点——checkBashCommand 换 classifyCommand；ask 档上下文挂起到写路径预检后 emit（deny 优先于 ask 播报）；permissionRequest 同构；gate-log rule 字段落分类器规则 id（自我插桩契约）。④tests/r6a.test.mjs 11 用例——单元（tokenizer 语义/wrapper 剥壳/sensitivePath 边界含模板豁免与反斜杠折叠）+集成（12 变形攻击全 deny 走真实 hook 通道 exit 2+stderr 规则 id：sudo/timeout/nice/env 穿透、bash -c 嵌套、管道外传三形态、融合参数、dd if、force push、chmod 777、fork bomb；7 正常命令全放行：npm test/curl GET/--force-with-lease/echo "git push --force"/grep 'rm -rf' 锚定/.env.example 模板豁免；ask 三档放行+observe 落账+additionalContext；项目附加正则生效；lint 改坏向量→exit 1 还原恢复；性能 1000 次混合分类 12ms）。⑤宪法 CLI 行+README 治理 CLI 一览补 classifier lint。终验：npm test 191/191（169 基线零回归+R6b 11+routing 11 在途并行态）；classifier lint exit 0（19 规则 146 向量）；性能锚点 0.013ms/命令均值（预算 5ms）；doctor/selftest/manifest 收尾重生成（自愈 R6b 呈报的在途哈希吸收）。行为变更如实声明：rm -rf <非根目标> 由放行收紧为 deny recursive-forced-deletion（codex 口径，派单 D 项明列）；cat .env.example 由 deny 放宽为 allow（模板白名单，cursor/codex 双仓口径，派单 E 项明列）；sudo <无害命令> 由 deny 转为 ask（提权知情，危险内层仍 deny——r4b 既有用例 sudo rm -rf /tmp/x 仍 exit 2 锁定）。

- 2026-09-02 修复 archive 字节预算死锁（red-locks 两轮闭环，implementer/tester/code-reviewer 子代理链）：症状=ledgerHealth 字节判超 × archiveLedger 纯条目数搬 → advice 空药方死锁（57778B/24000B 预算 moved:0）。修复=context.mjs 字节模式（keepMinDone=2/keepMinNotes=1 保留下限+投影搬迁+触底 overBudget 诚实上报，r3b T1-T3）；复审 P2 两项（F1 投影计入指针行成本——archivePointerLine 单点+willWrite 守卫；F2 movedSet→multiset 出现计数删除——重复文本不再击穿 keepMin 底线，T4/T5）；连带 supersede 语义（quality.mjs verifyLedger 同路径最新钉担责，r4fix2 T6a-d）解 seq72 钉可变文件雷、seq73 再钉收口。复审第 2 轮 ACCEPT 零 P1/P2。真实归档 moved=10（57778→32366B health 绿，指针链+幂等验证过）；npm test 200/200；selftest/receipt verify 全绿。.gitignore 补 .serena/.codegraph（工具本地状态非项目产物）。
- 2026-09-02 evolution-runner 毕业评估（7 候选+全量 20 条盘点，产出 11 提案待拍板）：6 条候选已机制化/规则化（P1 completion-claims→宪法纪律5+Stop门；P2 destructive-ops→纪律8+classifier deny 档；P3 main-agent-no-coding→纪律3/4+orchestration 三铁律；P4 red-locks→workflow 铁律+bug-fixer skill；P6 three-file-sync→sync-check 双缝+SessionStart；P7 waiver-lifecycle→五要素+到期自动失效），提案仅标 graduated；P5 single-truth-source 为唯一执法空白（「真相源」表述宪法/rules/ADR 契约 grep 零命中）——提案 ADR-Template 加「真相源对照」栏+graph.adrCheck 棘轮（新 ADR 缺栏 error，存量 8 个豁免）；P8 族A 聚类毕业（sibling-config+config-defects 合计 4）→code-review/bug-fixer skill 各加类别覆盖检查项；P9 族B 聚类毕业（read-deep+main-agent-verifies 合计 3）→宪法纪律5 加一句+orchestration 三铁律展开一句；P10 tag-check-remote-first 事实修正（条目声称 release-builder 已固化 ls-remote 实为不实）→skill 补该步；P11 减脂：.zcode/lib/ 7 个零调用方 shim（waivers/tasks/receipts/plan/budget/review/audit.mjs，grep 实证 0 引用）提案删除。拒绝留痕：metric-gaming+spec-overfitting 聚类 2<3 不达标；watching-hung 与 unbounded-retry 不同源不聚合；宪法禁用词与 workflow 话术规范语境不同不合并；Skill 新建零提案（高频模式已被 branch-finisher/dod 承载）。
- 2026-09-02 提交收口：catalog 补映射（progress.archive.md→governance；tests/r4fix2.test.mjs 显式列 quality-tests——tests/** 本已覆盖，显式行随同级惯例）+manifest 重生成（153 文件）+doctor 全绿 exit 0；主 Agent 直接提交（用户指令「不用代理」），commit-msg 质量门拦掉 fix 前缀空词后改动作式主题过闸；sync-check 拦 staged-catalog-无-progress-同步（铁律正确，本条即补记）。GitHub push protection 拦历史假夹具（mechanisms.test.mjs:265 的 Slack bot token 前缀+数字串教科书级测试串，commits a74fdf7/eb2bf55 的工作树早已改运行期拼装但历史 blob 仍在）——待用户走 unblock URL 或裁决历史改写。

## Next（下一步）

- CI 修复已 commit 待 push：push 后观察 `zcode-base gate` 4 矩阵作业（ubuntu/windows × node 22/24）全绿即最终验收；windows 若仍有残留红按 bug-fixer 熔断闸处理（≥3 次转根因）。push/tagging 均 HIGH 档待用户批准。
- evolution 提案 P1-P11 待用户逐项拍板（HIGH 审批，清单见 Open Issues；P5=ADR 真相源对照执法为最高价值，P1-P7 毕业标记可合并一张批准单）。
- git push 已完成（2026-09-02）：origin=https://github.com/zylimit/zcode-base.git，master@a5511b8 全量首推成功（含 v2.0 全历史），远端哈希核验一致；push protection 假夹具误报经用户 unblock 解锁。打 tag 仍待拍板（v2.0.0 候选，HIGH 档人类行为）。
- 二期候选：插件发行面（OQ-1）、hook 严格 JSON schema 实测校准（OQ-2）、模块胶囊补全（harness/modules/）。
- （已销）重启 ZCode 会话实测 SessionStart 注入——2026-09-02 本会话实证：恢复上下文自动注入+工作树脏警告在场，用户级 hooks 生效确认。

## Open Issues（未决）

- OQ-1 双形态（插件发行面）二期评估（Spec OQ-1）。
- OQ-2 hook 严格 JSON schema 字段名待客户端实测确认（当前保守契约已可用）。
- 2026-09-01 R3b 剩余地基层 + R3 修复轮完成：R3b（29 文件 +1818）——写路径预检（工具+shell 双路提取/symlink 逃逸无条件/ownedPaths 闸/knownHashes 并发冲突，来源 codex+cursor 融合）、budget 四指标+archive（append-only+幂等+阈值提示）、sync-check+3 git hooks（本仓接线狗粮，pre-commit 真跑过；按栈编译门 ccA7）、嵌套 AGENTS.md（riskTier+agents-lint+本仓 critical/high 两模块契约落地）、recap/invariants（SessionStart 切换+脏树校准 A4+feedback 播报 A5）、宪法三性/贷款同步；自纠 tokenizer 永真分支与归档顺序契约两处（测试先行抓出）。修复轮（10 文件 +255，R3a review F1-F5+验收 F13）——F1 债务逃逸链 red-locks 先红后绿（DEBT 改任务/窗口维度跨指纹存续+critical/high 档不可 skip=BLOCKED 语义+还清=后续非 SKIPPED 回执）、F2 quarantine 仅 SyntaxError、F3 E2BIG 响亮抛、F4 untracked 预算+16MiB 单文件上限+指纹 memoize、F5 tests/ 入软执法、F13 OUTSIDE_REPO 限活跃任务（无 task 放行，symlink 无条件保留）。npm test 69/69；doctor/selftest 0.156s/manifest 零漂移；本批 1919+255 行超 budget 默认限额（计划内 13-Task 地基批，显式说明而非拆分）。回执 seq 26-33 落账。
- 2026-09-01 Phase 9 R5b 落地（Task 9.4/9.5/9.6，implementer 子代理，与 R5a 文件面并行共享树）：①skills 增补——design-brief-builder 新建（视觉层采访协议：选择题优先/参考锚定/感受翻译/不问像素/联网优先/Spec 每个有 UI 功能须有页面+关键状态变体；依赖检测缺失 Spec→路由 product-spec-builder）+ dfx-designer 重写（12 维过堂=八属性+可服务性/可安装性/可测试性/可修改性各「定义/度量/对策/验证落点」一句、六要素场景逼可测、「可度量或不写」、双模式 Design-in+Review 评分卡、档位打架排优先级栈记被牺牲方）+ evolution-engine 扩四层进化（第④层 Skill 自动生成提案 ≥5 次重复模式、聚类毕业=同失败模式跨条目合计≥3 首例「远端实况实查」式、UX 三档记录无感/轻触提示/变更确认）+ bug-fixer 扩修复熔断闸（同一 bug 修复尝试≥3 次未转绿→强制回根因三问：架构假设/前提条件/怀疑层，与 risk scan FAIL-streak 呼应）；skills 17→18，skills-lint 全绿。②CI+工程配套——.github/workflows/gate.yml（dsh 结构：矩阵 ubuntu×windows×node 22/24 fail-fast:false fetch-depth:0；selftest 先行→install 冒烟注册临时 HOME（doctor 双通道输入+critical 模块冒烟）→doctor→skills-lint→scan-instructions→catalog lint→npm test→manifest check→dod→risk advisory→doctor always）；LICENSE MIT/根级 CHANGELOG（三 tag 记录）/[.editorconfig]/[.gitattributes]（LF 归一与 install 哈希配套）/package.json 补 license+engines>=20。③docs——OPERATING-MODEL（九阶段/四签字闸/审批三档/13 角色表/停止条件，对齐本仓宪法与真实命令面）/CAPABILITY-MATRIX（四仓 Absorbed/Adapted/Rejected 台账=dsh 22 项+三族增量按主题合并+明确不做清单 11 项+未来提案四规则）/ADOPTION（三起点+棕地十步序+day/week/month 表）/ADR-CONTRACT（Enforced-by 四类解析集+phantom 更糟+与 dsh 差异诚实边界）/CROSS-POLLINATION（周期化台账+首版快照）+ research 四报告入库副本（.zcode/docs/research/，9 行攻击样例加 scan-instructions:ignore 抑制留痕）+ ADR-0007 单目录封装/0008 fast 贷款语义（Enforced-by 全真实：doctor,selftest/quality verify,risk scan,receipt verify）+ ARCHITECTURE 索引补两行+skills 计数 18+fingerprint 描述修正（untracked 内容字节）。④测试联动——r4a 17→18 两处断言；r4b 本仓旧账本用例加干净检出 skip 守卫（.zcode/state 机器本地不随分支旅行——CI 干净检出的真实阻塞，隔离基线实证）。归因实证：HEAD+本批改动=147 测试 146 pass 0 fail 1 skip（无状态环境 skip 正确；本仓工作区该例执行且过）；共享树 5 失败（install/doctor hooks 两例+dod/release 三例）与 doctor managed-drift（context.mjs）均为 R5a 在途写入所致（其 mtime 晚于本批 manifest 重生成），未触碰呈报。CI 干净检出全步骤实跑：selftest/doctor(装后)/skills-lint/scan-instructions/catalog lint/manifest check/npm test/fitness/risk 全 0；dod exit 2=lib-core 属性接线缺口（R4e 遗留，R5a catalog 批次/Phase 10 终验收口——回执已注 CI 该步接线闭合前红属棘轮语义）。宪法/README 需 R5a 对齐行：skills 17→18、工作流路由表加 design-brief-builder 行、dfx 五性→八属性表述、「零依赖 Node ≥18」与 engines>=20 统一。
- 2026-09-01 R4 code-review FIX_REQUIRED 五项修复完成（red-locks 先行：tests/r4fix.test.mjs 8 用例 7 红 1 绿锁定后修复全绿）：F1(P2) review 引擎——recordLens 拒绝同 lens 重报（覆写=历史 error 无痕撤销）+ verdict 最终落定（ACCEPT+isFinal 或 escalate）置 session.final 封会话，封后 blue/lens/verdict 写操作 exit 1（backlog 不封=escalate 记债出路）；F2(P2) backlog 三性禁令扩结构化字段——lens 认领红线属性（LENS_LIBRARY×PROTECTED_ATTRS）即拒，location 文本并入 BACKLOG_FORBIDDEN 正则；F3(P2) install --verify stage 限安装面 pathspec（.zcode+AGENTS.md+progress.md+FRAMEWORK-MANIFEST.json，exclude .zcode/state）不再 git add -A——用户 untracked 保住（134 安装面 staged、user-draft.txt 仍 ??）；F4(P2) make-release SECRET_RE 对齐 scan.mjs SECRET_LITERAL_PATTERNS 十族（ERE 改写：\b→(^|[^[:alnum:]_]) 等；头注释声明共有局限 base64/分块/宽字符/无前缀串）；F5(P3) doctor.mjs:1 字面量 undefined 删除。顺手：invariants 法则 2 文案改「gate BLOCKED 非 PASS，按 exit 1 拒绝（2 是 hook 拦截保留码）」（实际在 context.mjs，派单写 core.mjs 系位置勘误）。基线勘误：派单称 161/161 实为 159/161——R5 commit b64d089 遗留（5 个根文件 UNMAPPED→catalog governance.globs 补归类；r5a fixture 明文需求 id 被 trace 本仓实扫当悬空→REQ@占位 unmask 运行期拼装）。F4 连带：mechanisms.test.mjs redactSecrets 单测 3 行明文假 token（JWT/postgres URI/xoxb）改运行期拼装（发布自验扫包内一切完整形态，测试源码不例外；工作树 tracked 面零命中，本仓 make-release dry-run 需 commit 后复验——包=HEAD 快照语义）。终验：npm test 169/169（161 基线+8 新增）；doctor/selftest/manifest/dod/receipt verify/quality verify 全 exit 0（gates 以 --executor tester 重跑三性+可靠性认领检查后 dod 转绿，budget 非阻断 modulesTouched 16>3 棘轮项留档）。
- 2026-09-02 R6 超越层完成（v2.0 全部批次收官）：R6a shell 语义分类器 v2 旗舰（.zcode/lib/classifier.mjs 426 行零依赖+19 规则 146 自测向量：tokenizer/wrapper 剥壳/嵌套 shell 递归/管道级秘密外传/融合参数 @file 提取/三档决策 deny-ask-allow；真实通道冒烟 12 变形攻击全 deny+7 正常全放行；性能 0.0093ms/命令；lint 自测首跑即抓出 --post-data= 漏配形态；行为变更三例按派单口径：rm -rf 非根收紧 deny/.env.example 放宽/sudo 无害 ask）；R6b 自我插桩 effectiveness（quality.mjs audit 节：每规则 deny/observe/allow 计数+unexercised 判定+blindSpot 标注——首跑真实数据：76 条 gate-log，rm-rf-root 拦 2/stop-gate 拦 2/sudo·git-force-push·write-preflight 各 1，「闸能说出它挡住过什么」落地）+live 路由断言库（tests/routing/ 三 fixture 对抗自验：good 过/premature 拒/cross-line 拒，断言只依赖三字段压小 OQ-5 校准面）+15 条三仓教训种子（feedback 20 条目，waiver-lifecycle 与 single-truth-source 成毕业候选）+宪法纪律 3 台账执法 OQ-4 括注。npm test 191/191；终验链 10 项 9 绿+dod attributes 项待干净树回执（本条提交后落账）。v2.0 六批（R1 结构/R3 地基/R4 体系/R5 扩展/R6 超越）全部交付。
- 2026-09-02 v2.0.0 终验收口：catalog 对齐（classifier.mjs→lib-hooks/classifier-rules→contracts/tests routing+r4fix+r6*→quality-tests；quality-tests→lib-quality 依赖边声明——arch 116 边零违例）；matrix reliability 认领 scope 扩 contracts+quality-tests；六属性认领回执 tester 落账（secret-scan/fitness-audit/harness-selftest/ledger-integrity）；dod 全绿 exit 0（12 步静态 DoD 首次全量闭合）。v2.0 六批全部交付：R1 单目录封装（6165b08）→R3 地基含 review 修复（eb2bf55）→R4 体系四批含 P2 修复（533b581/d8a9dff/1cd5d6d/b8b39e6+4e52574）→R5 扩展（b64d089）→R6 超越（4dd0c17）；测试 21→191；npm test/selftest/doctor/dod/classifier lint/effectiveness/recap/invariants/receipt verify/hook 模拟终验十项全绿。能力对 dsh 的超越点落地：三缝执法（ZCode hooks+git hooks+CI）、隐私入保护集、fast 已执行 FAIL 不可豁免（cursor 铁律叠乘）、shell 语义分类器 v2（四仓融合）、自我插桩 effectiveness（REQ-34）。
- 2026-09-02 deferred（复审第 2 轮 P3，均低危不阻断）：P3-1 F2 multiset 跨段错删——Done/Notes 存在逐字节相同条目行时段归属错乱（总量守恒、下轮归档自愈，E2 实验 keepDone=1/keepNotes=0 场景复现），修法=分段各建 Map+补跨段用例；P3-F3 prepend 归档段内顺序排版反转；P3-F4 同日二次归档重复 ## Archived 标题；P3-F6 ledgerHealth advice 文案 cfg.keepMinDone 未 ?? 兜底（显式 null 时显示 "null"，逻辑侧已有兜底）。
- 2026-09-02 evidence 钉用法注意：receipt write --evidence 钉可变源文件，演进后需更高 seq 再钉方回绿（supersede 已消永久雷，seq72→73 即实例）；正统用法是钉 gate 产物（state/evidence 三重句柄不可变）。
