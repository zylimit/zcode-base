# progress

## Pinned（长期约束）

- 宿主 = ZCode；只用原生扩展点（AGENTS.md/.zcode/用户级 hooks），规范源 = zcode-guide 官方 skills。
- runtime 零依赖 Node ≥18（ADR-0003）；hooks 单 dispatcher（ADR-0001）；账本哈希链 fail-closed（ADR-0002）。
- security/safety/privacy 三性永不可豁免、永不可 Fast 跳过；已执行的 FAIL 不可被 fast 豁免；Fast Mode 不豁免安全护栏。
- 家族资产保留复用：本仓七仓经验精华见 docs/ARCHITECTURE.md 与各 rules 头部来源标注。

## Decisions（决策流水）

- 2026-08-13 选 Workspace 布局+安装器形态（弃插件包：marketplace 摩擦大、skill 优先级最低）——用户拍板。
- 2026-08-13 硬门禁+留痕为默认强度（弃观察模式起步）——用户拍板。
- 2026-08-13 子代理角色契约放 docs/ROLE-CONTRACTS.md 而非 manifest agents 字段——zcode 记录但不执行该字段，诚实声明优于假装执行。
- 2026-08-13 hook 输出契约用退出码（0/2）+ additionalContext JSON——规避严格 schema 未知字段风险。
- 2026-08-13 commands 用嵌套目录 zbase/<verb>.md → /zbase:verb——Windows 文件名不允许冒号。
- 2026-08-13 git 路径枚举用 -z NUL 分隔（-z 已是无转义原始输出，无需 quote-path 选项）。
- 2026-08-13 Stop 门自计数封顶 2（ZCode 原生上限 3，留 1 次余量防死循环）。
- 2026-08-21 hooks 注册迁用户级 ~/.zcode/cli/config.json（ADR-0006）——工作区 hooks 每会话弹审核且无批量同意，门禁实测长期离线；用户级无审核即生效，wrapper 自检放行非 zcode 项目；doctor 双通道校验，工作区模式不堵死——用户拍板「迁移吧」。
- 2026-09-01 dsh-base 深研吸收（Spec v1.1，REQ-13..31，DEV-PLAN Phase 6-8 三批）：privacy 入永不可豁免保护集、Fast 改贷款语义（reason 必填/8h 封顶/DEBT 阻断）、recap+invariants 恒定成本恢复、sync-check+git hooks 补三文件同步执法缝、嵌套 AGENTS.md 模块契约、review 全链引擎化（结构化分歧）、八属性六档、需求追溯；fleet 多仓层暂缓（OQ-3）——用户指令「学习 dsh-base 优化 zcode-base 做到最好」；研究依据 .zcode/state/research/dsh-base-mechanisms.md（422 行源锚点报告）。
- 2026-09-01 升级为 v2.0 大重构（Spec v2.0，REQ-32..34）：单目录封装 .zcode/（学 dsh .dsh/ 哲学，安装面=一个目录+根级种子）、三缝执法（ZCode hooks+git hooks+CI）、自我插桩超越层（检查有效性计数——dsh 报告自评「治理处方几乎全部未被度量」正是缺口）；放弃 .agents/ 跨工具通用路径（宿主即 ZCode，深绑优于泛化）；DEV-PLAN 重排 R1-R6 六批，R1 纯结构迁移先行——用户指令「不计成本、大规模重构、比 dsh 更好」。并行三路家族仓增量研究（cursor/codex/cc-base，产出 .zcode/state/research/*-delta.md）+ 联网输入（agents.md 标准、Martin Fowler SDD 对比、OpenAI 治理 Cookbook）。
- 2026-09-01 cursor-base 增量裁决（报告 .zcode/state/research/cursor-base-delta.md）：①fast mode 之争两案叠乘——保留 dsh 贷款四条件 + 叠加铁律「已执行的 FAIL 永不可 fast 豁免」（跳过未运行≠豁免已证缺陷），超越两家；②增量并入批次：R3=withStateLock 状态锁/redactSecrets 输出脱敏/task known_hashes 写前冲突检测（纪律 2 执法化）/quarantine 隔离/boundedHead·PASS 静默·空计划 BLOCKED 杂项；R4=账本轮转+anchor/retention 引用保护/FAIL-streak 根因重定向/feedback 引擎化 lint/install 三方合并 upgrade·uninstall+safeManagedPath/waiver Compensation·Approval/性能锚点测试+256MB maxBuffer；R5=adapters 八属性证据供给侧/runtimeValidityHours 时间窗/plan_sha256；R6=shell 语义解析器+管道级秘密外传检测；③立场吸收：「检查优先于常驻文本」为宪法体量标尺、progress 模板加 Not doing 段、教训库不随 install 旅行、config 形缺陷同类实例复发必搜（feedback 种子）；④service 监督不整体移植（摘 probeHealth 语义 + 重启风暴熔断教训）。
- 2026-09-01 codex/cc 增量裁决（codex-base-delta.md 473 行 / cc-base-delta.md 265 行）：①缺陷修复级必做（R3）——跨进程状态锁（两仓独立同证）、untracked 内容入指纹（WIP 期证据绑定盲区）、全局输出脱敏 13 类（receipt note 现原样落 token）；②最高价值（R4）——verification plan（该跑什么=impact 确定性函数+planHash）、evidence 三重完整性、executor 角色绑定（一行执法宪法第 4 条）、completion 完成门聚合、install 事务性三仓合流；③cc 会话闸门（R3）——three-file-sync 挂 Stop/recap-on-dirty/check-evolution 播报/按栈编译门；plan-lint/skill 触发式描述③④/test-routing 双向一致性/CoVe verificationQuestion（R4）；make-release 泄漏自验（R4）、adapters 表（R5）、design-brief/dfx 12 维六要素/evolution 第④层/修复熔断（R5）；④R6 旗舰=shell 语义分类器 v2 四仓融合（tokenizer+管道外传+融合参数+三档决策+match/notMatch 自测向量）；⑤明确不做——service 监督（三仓共识拒绝）、auto-push、SubagentStop 机制（ZCode 无此事件已查证）、sh/ps1 双写（Node 免疫）、path lease（缓）；⑥新 OQ-4/OQ-5；⑦关键教训——量化指标否决（specification overfitting）、审查承重墙是外部证据非对抗形式、双真相源拒斥。
- 2026-09-01 R1 结构重构完成（M4 里程碑）：脚手架本体全收 .zcode/（lib×19/skills×17/commands×16/feedback/rules/docs/harness/state 运行态 gitignored）；109 项变更 git rename 保历史；55+ 文件路径切换零残留；install 单目录化（隔离冒烟 96 文件）；manifest 99 文件零漂移。主 Agent 新鲜验收：npm test/doctor/selftest 0.21s/manifest/hook 全 exit 0；用户级 wrapper 8 处已切 .zcode/zbase.mjs（双环境冒烟过）。DEV-PLAN v2.0 定稿：Phase 7=13 Task 地基（R3）、Phase 8=11 Task 体系（R4）、Phase 9=6 Task 扩展（R5）、Phase 10=5 Task 超越+终验（R6），每 Task 标源仓锚点。
- 2026-09-01 F1 修法裁决（R3a review 实证 P1：fast 债务收口逃逸链——SKIPPED 落账后改一行代码，fastDebtReceipts 按 fresh 过滤致债务随指纹漂移蒸发，task finish 无痕通过；叠加 critical 档可被 fast skip 的档位语义缺口）：①DEBT 查询改任务/窗口维度——不按 fingerprint fresh 过滤，窗口内产生的 SKIPPED 债务持续到还清（risk scan 同理）；②critical/high 档 check 不可 fast skip——ENFORCE_LEVELS 判定前置于 fastSkipValid，blocking 档在 fast 窗口内未跑=BLOCKED 语义非 SKIPPED（与 rules/quality-attributes.md「可跳：medium/low 档」对齐）；③red-locks：先锁「跳过后改代码再 finish 必须阻断」+「critical 档不可 skip」两条失败测试再修。修复与 R3b 串行（同文件并发写冲突），R3b 验收后统一发修复单（F1-F5：另含 quarantine 仅 SyntaxError 隔离/E2BIG 响亮失败/untracked 预算+memoize/tests 入软执法）。

## Done（完成流水）

- 2026-08-13 Phase 1-4 全量交付：宪法/rules×4、runtime（zbase.mjs+lib×19）、.zcode hooks 注册、skills×17、commands×16、feedback×6、docs×5+ADR×5、harness 契约（catalog/matrix/schemas×6/templates×9）、自举 Spec/PLAN。
- 2026-08-13 终验全绿：tests 21/21；doctor 16 项全 PASS（归类 104 路径）；selftest 120 模块×30k 路径 722ms（预算 2500ms）；7 hook 事件模拟（deny/additionalContext/放行）全部符合契约；matrix 8 项门禁全 PASS 落账（receipt #9-16）；quality verify ok（covered 7 / blocking 0）；arch check 零违例（7 条真实边全声明，空基线起步）；adr check 零幽灵引用；install 双向验证（首装 94 文件 + 定制旁路 .zbase-new + 目标 doctor 全绿）；manifest 95 文件零漂移。
- 2026-08-21 hooks 用户级迁移落地：本机手工迁移（8 条 wrapper 双环境冒烟：zbase 项目透传/无 runtime 静默 exit 0）+ 工作区 .zcode/config.json 清空；脚手架同步（implementer 子代理）：install 新增 registerUserHooks()（只覆写 hooks 键/mcp 保留/幂等/原子写/损坏 fail-visible）+ doctor 双通道 + tests 24→25 用例 + manifest 重生成（96 文件）；code-reviewer 三阶段审查裁定 FIX_REQUIRED 仅 F1（覆写第三方 hooks 无备份，已修复：异已 hooks 整文件备份 config.json.bak-zbase-<ts> + report 可见告警 + 2 用例断言）；文档同步 6 处矛盾 + ADR-0006 + ADR 索引。终验新鲜证据：tests 25/25 exit 0；doctor ok exit 0（hooks 用户级通道 PASS）；manifest check ok exit 0（96 tracked）；adr check ok exit 0（6 ADR 零幽灵）。备份集中 ~/.zcode/backups/（工作区+用户级各一份，含回滚材料）。已知边界：wrapper 与备份文件名 POSIX-only（Windows 不适用，ADR-0006 声明）。
- 2026-09-01 R3a 引擎核心卫生层完成（commit a74fdf7，17 文件 +1024/-158）：PROTECTED 三性/fast 贷款（minutes≤480+reason 必填+windowId+allowFastSkip+SKIPPED 留痕+DEBT 阻断）/跨进程状态锁（3×25 并发零丢失）+quarantine/untracked 内容字节入指纹（变化实证）/全局脱敏 16 模式三出口（0 残留）/护栏软执法+输出预算/Stop 分键多槽 LRU。tests 41/41、doctor/selftest 0.19s/manifest 零漂移。code-reviewer 三阶段裁定 FIX_REQUIRED（F1 实证 P1 债务逃逸链 + F2-F5 应修，裁决与修复计划见当日 Decisions；F7 文档残留清单交 R3b 7.13 统步）。
- 2026-09-01 Phase 7 R3b 落地（Task 7.6/7.9/7.10/7.11/7.12/7.13，implementer 子代理，25 文件）：①写路径预检 lib/writes.mjs——工具+shell 写路径提取（轻量 tokenizer+引号状态机，$VAR/*/~/dev 汇不提取）/apply_patch 解析/symlink 逃逸（最深存在祖先 realpath）/ownedPaths 闸（glob 与字面前缀双兼容）/knownHashes 并发冲突三态（任务外写=TASK_SCOPE、存在不在基线=TASK_NEW_FILE_CONFLICT、哈希≠基线=TASK_CONCURRENT_CHANGE）；task start 对 owned∪tracked∪dirty 过滤到信封内建 digest 基线+preexistingDirty，PostToolUse refreshTask 刷新基线与 touchedPaths；②lib/budget.mjs 四指标（numstat 累加/impact 直接受影响模块/untracked 计数，harness.json budget 可覆盖，超限 exit 1）+ lib/memory.mjs archive append-only（保最新 40 Done/30 Notes，指针行不计条目防二次搬指针，幂等）+ ledgerHealth（M3 阈值 Done>100 提示自动归档）；③lib/sync.mjs（MEMORY_BEHIND_CODE/SPEC_WITHOUT_CHANGELOG；progress.md 在盘才执法——「不存在的不强造」，无盘降 LEDGER_NOT_CREATED 提示）挂 Stop 门（与回执门共用分键三振，ruler='three-file-sync'）+ recorder 豁免（.zcode/state/.progress-recording 标志 或 progress.md 2s mtime 窗口；选型=skills 不在本批 Scope，取 hooks 侧实现）+ 3 git hooks（.zcode/githooks/ POSIX sh+chmod +x：pre-commit=sync-check --staged+staged 秘密扫描+按栈编译门 ts→tsc/py→ruff 降级 py_compile 工具缺失跳过；commit-msg=<12 拒/空词拒/>72 警；pre-push=doctor+manifest，exit 3 翻译 degraded 非 pass）+ install --hooks（core.hooksPath 接线，他方值 warning 不覆盖）+ doctor git-hooks 校验项（PASS 注明可选）+ 本仓狗粮接线 core.hooksPath=.zcode/githooks；④嵌套 AGENTS.md：module-catalog schema+lint 加 riskTier（BAD_RISK_TIER）+ 本仓 8 模块定档（runtime-harness=critical/contracts=high/governance·installer·quality-tests=medium/skills·commands·feedback=low）+ lib/agentslint.mjs（high/critical 无契约=NO_MODULE_AGENTS error；四段缺=MODULE_AGENTS_INCOMPLETE warning；>12KB warning）+ MODULE-AGENTS 模板 + .zcode/lib/AGENTS.md 与 .zcode/harness/AGENTS.md 真契约；⑤recap/invariants（6000/1200 预算，blocks=Position/Pinned12/In progress/Next P0P1/Decisions5/Done6/Risks/Decay signals+ledgerHealth；行 clip 200 硬截断标 truncated；SessionStart 注入切 recap 预算 3600+工作树非干净校准提醒 A4+待毕业 feedback 播报 A5）；⑥宪法/rules/README 同步（Fast Mode 贷款语义/三性红线+privacy/命令参考+检查优先于常驻文本/workflow Enforced-by/quality-attributes 三性不可豁免+FAIL 不可 fast 豁免/README CLI 一览 7 行）；PROGRESS 模板不存在按派单指示跳过。测试 41→60 全绿（tests/r3b.test.mjs 19 用例）。自纠两处实现缺陷：shell tokenizer `|| '<'` 永真分支（全部字符成操作符、提取恒空——测试先行抓出）、归档顺序契约（本仓 append 式最新在尾，dsh prepend 语义会搬走最新——改 order:'append' 搬头留尾，recap Recent 同步取尾）。

- 2026-09-01 Phase 8 R4b 证据与计划核心落地（Task 8.3/8.4，implementer 子代理，与 R4a 文件面互斥并行）：①verification plan（新 lib/plan.mjs）——组队=riskChecks[task.risk] 起始组×受影响模块（impact 反向闭包）module.verification 并集×保守扩散（degraded→conservativeChecks）×依赖传递闭包，拓扑序输出+环检测（MATRIX_CYCLE exit 1），每 check 携带 reasons（risk:<档>/module:<id>/conservative-impact/dependency-of:<id>）可追溯，planHash=sha256(canonicalJson(选中集+reasons))；CLI 新增 plan verb；matrix check 新字段 dependencies/resourceLocks/platform（schema 同步，本仓 matrix 补 riskChecks/conservativeChecks、catalog 5 模块补 verification 映射）；兼容开关=未声明组队面（无 riskChecks/conservativeChecks/module.verification）→ PLAN_NOT_ADOPTED 传统模式零迁移；空计划显式 empty（配置失败不是绿灯）→ gate 拒跑/quality verify critical 阻断/task finish 拦；gate 执法：CHECK_NOT_PLANNED 拒未组队检查、依赖最新新鲜回执非 PASS→BLOCKED「dependency did not pass」落账、平台不符→BLOCKED、resourceLocks 经 withStateLock 命名空间 resource-locks/<name>；回执带 planHash（计划选择变化→旧回执不匹配，stale 语义留 8.5 completion 门消费）。②evidence 三重完整性——gate 全量输出先 redactSecrets 再 boundedTail 200000 保尾，原子写 .zcode/state/evidence/<task>/<check>-<ts>-<pid>.log，回执新增 evidencePath/evidenceBytes/evidenceHash（哈希链覆盖；旧回执缺省兼容+legacy 标注，本仓 33 条存量验证通过）；verifyLedger 逐条复验：相对路径无 ..（EVIDENCE_PATH_UNSAFE）→ realpath 落 evidence 内（EVIDENCE_PATH_ESCAPE）→ 逐字节长+sha256（EVIDENCE_TAMPERED/MISSING）→ fail-closed exit 4。③账本轮转——保留最新 500 条（harness.json ledger.rotateKeep 参数化，≤0 关闭），anchor=最后被丢弃条目链值（sidecar ledger.anchor.json），保留尾部从 anchor 端到端可验证，anchor 损坏/缺失 fail-visible；gate-log 同款尺寸轮转（4MB→.1 保一代，retention.gateLogMaxBytes）。④retention 引用保护——protectedPaths=当前指纹回执+每 (task,check) 最新回执+保留账本任一引用（超集必须：verifyLedger 全账本复验，删被引用 evidence=自造 EVIDENCE_MISSING）；.corrupt-* 取证永不删；prune 支持 --dry-run。测试 69→85 全绿（tests/r4b.test.mjs 16 用例：组队/闭包/保守扩散/环/空计划/CHECK_NOT_PLANNED/依赖与平台 BLOCKED/资源锁在场/planHash 变化/evidence 篡改三态/路径逃逸伪造/引用保护/轮转 anchor/旧账本兼容）；宪法+README CLI 同步。边界：manifest 因并行批共享树重生成（115 文件，含 R4a 在途件）；audit.mjs 加 3 行轮转调用（gate-log 写入点，非审计逻辑）；r4a.test.mjs 3 例在途失败属并行批次。

- 2026-09-01 Phase 8 R4c 落地（Task 8.5/8.6/8.11，implementer 子代理）：①review 全链引擎化（新 lib/review.mjs + zbase.mjs review/review-pack verb）——start=impact(fanout 保守)×profile 组队（personal=1/team=3/production=5/regulated=5，LENS_LIBRARY 五 lens：correctness(s1 无属性永留)/reliability(s2)/resilience·security·privacy(s3 trust)）×属性裁剪（lens 属性无受影响模块声明 low 以上不召集，correctness 除外；excludedLenses 各带理由）+空树拒绝(degraded 3)+上一会话 FIX_REQUIRED→lineage(diffHash/errors/round)；blue=stdin JSON claims 协议（claim 缺 evidence 拒——没有命令/路径/退出码的主张只是观点）；lens=findings 必须 file:line 或可跑 reproduction 否则整批拒+severity 枚举+stage 门（当前 stage required 未报完不得开下一 stage，stageGated exit 1；空 stage 跳过）；verdict=freshness(fingerprint 变→一切写操作 stale exit 4)+errors 跨 stage 聚合→ACCEPT(0)/FIX_REQUIRED(2)/NEEDS_MORE_EVIDENCE(3)+unable→NEEDS_MORE_EVIDENCE+maxRounds(catalog.review 默认 3)超限 escalate STOP 建议+isFinal=stage3 或无更深+仅 ACCEPT+isFinal 自动落 check=review 回执（extra 字段 reviewVerdict/reviewScope/lenses 进哈希链——writeReceipt 扩 extra 透传，修参数解构丢字段缺陷）+advice 含「共识比三个分歧的 lens 更差」；backlog add/list（三性词汇 /(security|safety|privacy|pii|secret|credential)/i 拒绝 exit 1；EXPIRED 标注）；review-pack（base 解析 tag→origin/main→首 commit；Commits/Diffstat/删除审计/Untracked/Diff；>800 行溢写 patch 留指针）；CoVe 扩展=findings 可选 verificationQuestion→verdict 输出 pendingVerification「待独立核验」（Judge 前独立验证者消费）。②completion 门聚合（quality.mjs completionStatus，task finish 消费）——required(plan 组队)检查逐项可接受（新鲜 PASS/同窗口 fast SKIPPED/未过期 waiver）+planHash 消费（R4b 留口闭合：回执 planHash≠当前 plan→stale 需重验，plan 采纳后手动 receipt write 无 planHash 同样不算）+optional 组队外已执行 FAIL 阻断（已执行的失败永不可接受）+review 门（catalog.review.requireForFinish===true 采纳开关且 risk∈{medium,high} 无 fast 时要求新鲜 ACCEPT review 回执+scopeMatches 排序比对 ownedPaths（范围过期不算）+无未解 error）——采纳开关沿用 PLAN_NOT_ADOPTED 哲学（宪法 red-blue 为条件触发路由，默认强制 medium/high 全量审查与宪法冲突且破 104 回归，呈报主 Agent 知悉）；③executor 绑定（宪法纪律 4 机器执法）——receipt write/gate 加 --executor（role 正则 ^[a-z][a-z0-9-]{0,31}$）回执记 executorRole，risk=high 无 fast 且 required 回执 executorRole!=='tester'→finish 拒；④waiver 契约扩 Approval 可选字段（审批发生处——审计记录非身份证明；schema+模板同步，compensation 已有保留必填）+模板措辞对齐三性；⑤red-blue-review skill 重写为引擎协议版（blue/lens/verdict 全走引擎 stdin 协议、lens 独立 fresh 子代理派单、skill 封顶 2 轮与引擎 maxRounds=3 协调、CoVe Judge 前核验、三性不可 backlog）+宪法路由行同步引擎措辞+README CLI 两行（lib 模块契约义务）。测试 104→118 全绿（tests/r4c.test.mjs 14 用例：组队裁剪/空树/blue 协议/lens 四拒/stale 4/全链 ACCEPT 落账/FIX_REQUIRED lineage/maxRounds escalate/backlog 三性/review-pack 溢写/optional FAIL/非 tester 拒+tester 放行/planHash 消费（伪造 risk 档变）/scope 不匹配+匹配放行/回归面）；隔离项目全链冒烟（start 组队→blue 拒→stage 门拒→报齐 ACCEPT seq 落账→FIX_REQUIRED lineage→escalate→stale 4→三性 backlog 拒→completion 三拒一放行）；doctor/selftest/skills-lint/scan-instructions/test-routing/fitness scan/manifest check(116 文件) 全 exit 0，receipt verify 链完整（33 legacy 兼容）。回执 seq 34（review ACCEPT smoke）落账。
- 2026-09-01 Phase 8 R4d 落地（Task 8.7/8.8 release+dod+make-release + install 三仓大合流，implementer 子代理）：①release（新 lib/release.mjs + zbase release verb）——九条件证据装配（dod-static 八项内部聚合/trace-coverage legacy degraded 放行/ledger-intact/receipt-fresh=当前 fingerprint 下有新鲜回执/fast-mode-closed/fast-debt-repaid=全量 SKIPPED 债务核销/review-backlog 过期非阻断/decay-signals 非阻断/sync-clean），7 阻断+2 非阻断，READY exit 0/NOT READY exit 2，报告内置「tagging/pushing/deploying 是 HIGH 档人类行为，本命令永不执行」。②dod——12 步静态聚合（catalog/skills/agents/rules-audit(advisory 非阻断)/adr/attributes(quality verify)/arch/fitness/trace(legacy 放行)/ledger/risk(非阻断)/budget(非阻断)），每步 try-catch 引擎错误→DEGRADED 标注不假绿，blocking 失败 exit 2。③make-release（新 .zcode/scripts/make-release.sh，POSIX sh set -eu + trap EXIT fail-closed；trap ERR/set -E 属 bash 专属，POSIX 等价实现并在头注注明）——git archive HEAD 解包→私人内容剥离（feedback 顶层 *.md 删、FEEDBACK-INDEX 重置内联干净模板）→tar.gz 打包（MINGW 分支 python3 zipfile，无则 exit 1 不发半包；该分支本环境不可测，回执如实标注）→打包后泄漏自验三面（feedback 私条目/运行态 .zcode/state|.zbase/秘密完整形态 AKIA·sk-·ghp_·PEM——锚定完整形态，引擎与钩子里的模式源码文本前缀后跟 [ 天然不误命中，本仓 tracked 全量预扫零自命中）→命中 exit 1 且删坏包；--dry-run 扫剥离后树零写报清单。④install 大合流（lib/doctor.mjs install 区重写为 plan/apply 分离）——plan 逐文件 LF 归一化哈希三态（unchanged/create/update/conflict-旁路 .zbase-new）+三方合并 obsolete 两态（未改 remove-obsolete/改过 preserve-obsolete）+种子 create-only；apply 每 mutation 备份进**目标仓外** os.tmpdir staging→执行→整体 post-verify 逐文件 LF digest 复核→失败逆序回滚→install-receipt 三态（committed/rolled-back/rollback-incomplete）落目标仓外临时文件+stdout 报告（不污染目标仓；incomplete 保留 staging 备份并在回执记 stagingPreserved）；故障注入 env zbase-install-fail-after=N；safeManagedPath 反穿越（绝对路径/../空段/反斜杠拒绝+逐段 lstat/realpath 校验仍在目标内+悬空 symlink 报错）；--dry-run 全程零写（含用户级配置与目标目录创建）；--verify 先 git add -A（非 git 仓 warning）再子进程跑安装副本 doctor(取 failing→warnings)/selftest/skills-lint(失败→errors)/catalog-lint(骨架态→warning；0 tracked→warning 什么都没证明)；--targets-from FILE 批量单目标失败不中断；--json stdout 恰一行（人类诊断走 stderr 通道分离）；uninstall 事务性只删仍等于基线的受管文件+清空目录+定制留置。既有语义保留：registerUserHooks（含备份）先行且不参与回滚（用户级配置全局共享幂等；dry-run 一并跳写）、旁路永不覆盖；manifest 基线改为按实际安装面生成（修复旧版整仓复制源 manifest 使 README/setup.sh 等从未安装文件进目标基线的潜在误判；obsolete 判定限定 .zcode/+AGENTS.md 面）。⑤宪法命令参考补行（dod/release/install/make-release；exit 2 语义扩「发布门阻断」）+ README CLI 4 行 + usage。测试 118→140 全绿（tests/r4d.test.mjs 22 用例：dod 三态/release READY·三阻断逐项·债务阻断·过期积压非阻断/make-release 冒烟·dry-run 零写·嵌套泄漏注入·秘密运行期拼装注入·运行态注入全部 exit 1 不发坏包/install dry-run 零写·json 恰一行·幂等·LF 归一 CRLF 不误报且不重写目标·obsolete 两态·旁路永不覆盖·fail-after 回滚完整·0444 恢复失败 rollback-incomplete·verify 先 stage·批量·uninstall 两段·safeManagedPath 穿越六态）。自纠两处：make-release 测试 helper 参数序（--dry-run 误入 env 位）；rollback-incomplete 用例改 chmod 文件 0444（实证：覆写已存在文件只需文件写权，chmod 目录 0555 拦不住 update）。本仓实跑如实：dod exit 2（attributes 阻断——runtime-harness critical 档无新鲜 PASS 回执，本批变更使指纹漂移，属预期 stale 态）；release NOT READY（dod-static/receipt-fresh/sync-clean 三阻断，同因）；修复 dsh 参照时对齐既有契约——zcode exit 2 原文档语义「hook 阻断」扩为「hook 阻断与发布门阻断」并同步宪法/README。
- 2026-09-01 Phase 8 R4a 检查面扩展完成（Task 8.1/8.2/8.9，implementer 子代理，与 R4b 文件面互斥并行，合计 104/104 全绿）：①skills-lint（新 lib/skillslint.mjs）——手写 frontmatter 解析（引号剥壳/>- 与 | 块标量折叠/缩进续行）+ 校验码（NO_SKILL_MD/BAD_FRONTMATTER/NO_NAME/NAME_NOT_KEBAB/NAME_MISMATCH/DESCRIPTION_TOO_LONG >500err>220warn/CAMEL_CASE_KEY/SKILL_LARGE >24KB/DUPLICATE_SKILL）+ cc 触发式描述③（无「当…时/…时使用/由…调用」→warning）④（无触发条件且以生成/支持/分阶段/输出/执行/维护开头作主体→warning）；本仓 17 skills 实扫 0 error 0 warning。②scan-instructions（新 lib/scaninstr.mjs）——六类指令面（AGENTS.md/skills/*/SKILL.md/commands/**/rules/docs/feedback）八规则行级扫描（endpoint-override/embedded-credential/instruction-override/exfiltration-command/silent-execution/hidden-characters/gate-disable=error，secret-file-read=warning），scan-instructions:ignore 上一行/本行抑制，>1MB 跳过+warning；本仓 55 文件实扫零命中；修正 dsh 原版 latent bug——前导连字符旗标（--no-verify/curl -d）前置 \b 永无边界（空格与-均非词字符），改 (?:^|\s) 定位（原版两规则对旗标形态实测全漏检）。③rules-audit（新 lib/rulesaudit.mjs）——规则行=编号/圆点/表格行 ≥25 字符（跳 code fence），执法点=反引号 token 剥 `node .zcode/zbase.mjs `/`zbase ` 前缀后命中 known 集，known 集全动态（zbase.mjs case 正则枚举 ∪ usage 子命令 ∪ matrix 检查名 ∪ fitness AUDIT_IDS/SCAN_RULE_IDS 导入，不硬编码防 PHANTOM）；三态 enforced/declared-unenforced（行内或下一行 prompt-only）/unenforced（error finding），advisory 默认 --max N 设限后阻断；本仓实扫 34 规则行 enforced 5 / unenforced 29 / ratio 0.147——宪法执法覆盖现状如实入账（工作流路由 15/核心纪律 7/诚实边界 3 等段未绑执法点，按「优先毕业为检查」逐步消化）。④test-routing（并入）——路由表声明 skill ↔ 实体双向（幽灵=error/孤儿=warning）+ 常用动词 ↔ zbase case（幽灵命令=error）；本仓幽灵 0/孤儿 1（skill-builder 未入路由表，warning）/幽灵命令 0。⑤plan-lint（并入）——占位词九类（fence 跳过）+ 每 ## Phase 须含 Task 表（验证列 error/风险列 warning，Phase 5 历史表无风险列降级适配）+ ≥1 Task 行；本仓 DEV-PLAN 10 Phase exit 0。⑥fitness scan 子命令（lib/fitness.mjs 扩展与 F1-F5 接线审计并存）——五反模式（no-secret-literal error 高置信窄面与脱敏解耦/no-pii-in-logs error/empty-catch warning 含 except:pass/unbounded-retry warning/todo-without-owner info）+ zbase-fitness:ignore 行内/上一行抑制（抑制留在 diff 可见）+ 只扫变更路径（2000 文件/1MB/200 findings 封顶标 truncated）；本批文件零命中（fitness 自身规则行用 ignore 注释自举）。⑦managedDrift（lib/doctor.mjs）——manifest LF 归一化 digest 逐文件比对，critical 档（config/harness.json/catalog/matrix/lib/githooks）漂移=FAIL、customized=注明；bootstrap 出厂态（catalog 空骨架/matrix 仍 starter→warning 不堵新装）；本仓 doctor 零漂移 115 文件 + bootstrap PASS 已定制。⑧FAIL-streak（lib/risk.mjs）——账本按 check 分组取尾连续 FAIL≥3 → high「停止重跑转根因分析（bug-fixer）」，夹其他 check 回执不打断连击。⑨feedback 引擎化（新 lib/feedbacklint.mjs）——契约（id=文件名/occurrences 正整数/graduated bool）破坏 exit 1；list 毕业候选（≥3 未毕业）；risk 播报 FEEDBACK_GRADUATION_PENDING（info）；本仓 5 条种子补 frontmatter（occurrences=3 按实情/graduated=false 按派单，正文「已机制化」为历史注记，正式毕业待 evolution-runner）→ 5 条全候选。⑩宪法命令参考补行（AGENTS.md 常用动词 +9）+ README CLI 一览 9 行 + usage。测试 85→104（tests/r4a.test.mjs 19 用例；新 case 自动进 rules-audit known 集）。R3b 教训推广：测试 fixture 反模式行运行期拼装且需拆行/改名（变量名含 retry、注释含字面量形态均自命中）；并行批 tests/r4b.test.mjs:358 字面量 token 触发 no-secret-literal 2error+empty-catch 1warning——fitness scan 本仓 exit 3 属该文件，按派单未触碰，呈报主 Agent（建议该批运行期拼装或加 ignore）。manifest 收尾重生成 115 文件零漂移（共享树，R4b 若再改需再生成）。

## Next（下一步）

- 重启 ZCode 会话实测：确认无「待审核」提示、SessionStart 注入恢复上下文（用户级 hooks 生效的最终证据）。
- git push + 打 tag（v1.0.0 或并入后 v1.1.0）——remote 未配置，待用户提供地址；hooks 迁移变更已提交本仓（2026-08-21）。
- 二期候选：插件发行面（OQ-1）、hook 严格 JSON schema 实测校准（OQ-2）、模块胶囊补全（harness/modules/）。

## Open Issues（未决）

- OQ-1 双形态（插件发行面）二期评估（Spec OQ-1）。
- OQ-2 hook 严格 JSON schema 字段名待客户端实测确认（当前保守契约已可用）。
- 2026-09-01 R3b 剩余地基层 + R3 修复轮完成：R3b（29 文件 +1818）——写路径预检（工具+shell 双路提取/symlink 逃逸无条件/ownedPaths 闸/knownHashes 并发冲突，来源 codex+cursor 融合）、budget 四指标+archive（append-only+幂等+阈值提示）、sync-check+3 git hooks（本仓接线狗粮，pre-commit 真跑过；按栈编译门 ccA7）、嵌套 AGENTS.md（riskTier+agents-lint+本仓 critical/high 两模块契约落地）、recap/invariants（SessionStart 切换+脏树校准 A4+feedback 播报 A5）、宪法三性/贷款同步；自纠 tokenizer 永真分支与归档顺序契约两处（测试先行抓出）。修复轮（10 文件 +255，R3a review F1-F5+验收 F13）——F1 债务逃逸链 red-locks 先红后绿（DEBT 改任务/窗口维度跨指纹存续+critical/high 档不可 skip=BLOCKED 语义+还清=后续非 SKIPPED 回执）、F2 quarantine 仅 SyntaxError、F3 E2BIG 响亮抛、F4 untracked 预算+16MiB 单文件上限+指纹 memoize、F5 tests/ 入软执法、F13 OUTSIDE_REPO 限活跃任务（无 task 放行，symlink 无条件保留）。npm test 69/69；doctor/selftest 0.156s/manifest 零漂移；本批 1919+255 行超 budget 默认限额（计划内 13-Task 地基批，显式说明而非拆分）。回执 seq 26-33 落账。
