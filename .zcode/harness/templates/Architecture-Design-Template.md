# Architecture-Design — <项目名>

版本: v0.1 ｜ 状态: Draft | Signed ｜ 日期: YYYY-MM-DD

## 1. 需求映射

<!--
  作者规则（arch-designer 消费）：「业务诉求」列从 Spec「业务上下文」章节逐条摘一句
  （为什么做/谁受益/例外——不重写，摘原文），不许写「提升用户体验」类空话。
  摘不出 = Spec 业务上下文没写实 → 退回 product-spec-builder，不在空地基上设计。
-->

| Spec 条目 | 业务诉求（从业务上下文摘一句） | 架构承接 |
|---|---|---|
| REQ-1 | <谁在什么情境需要什么，一句> | … |

## 2. 业务推导

<!--
  作者规则（arch-designer 消费）：架构从业务推导，不从技术流行度推导。逐项过五问：
  业务职责（这个模块替谁挡了什么事）/ 数据归属（这份数据事实上属于谁的业务）/
  一致性要求（业务能容忍多久不一致、丢多少）/ 故障后果（这块挂了谁的什么业务停摆）/
  团队能力（谁来维护，这个复杂度谁兜得住）。
  每个重大取舍写一行「业务理由→技术选择→被牺牲方」——说不清被牺牲方的取舍是没做过取舍。
  这是 DFX 优先级栈的前移：八属性定档发生在架构决策之后就是顺序倒置——
  先在架构层问业务优先级，DFX 阶段只做档位细化与指标化。
-->

| 取舍 | 业务理由 | 技术选择 | 被牺牲方 |
|---|---|---|---|
| <一句话取舍点> | <五问里的哪几问逼的> | <选了什么> | <明确放弃什么、谁承担代价> |

## 3. 分层与模块

```
<层级图：layers 自上而下，依赖只许向下>
```

模块清单落 `.zcode/harness/module-catalog.json`（M/L 档项目在此阶段产出骨架并 `catalog lint` 通过）。

## 4. 关键决策（ADR 索引）

| 决策 | ADR |
|---|---|
| … | docs/adr/0001-… |

## 5. 接口契约

<公共 API / 事件 / 数据 schema；破坏性变更流程>

## 6. 五性承接（DFX 前置）

| 属性 | 档位 | 初步手段 |
|---|---|---|
| 韧性 | … | … |

## 7. 大仓策略

- 预估规模：文件数/行数/模块数
- 归类策略：catchAll 是否启用、global 列表
- 单 writer 资产：共享契约/schema/迁移/lockfile

## 签字

- [ ] 用户确认（批的是当前这版内容；变更须重新请批）
