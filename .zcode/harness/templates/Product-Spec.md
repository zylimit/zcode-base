# Product-Spec — <项目名>

版本: v0.1 ｜ 状态: Draft ｜ 日期: <YYYY-MM-DD>

<!--
  作者规则（product-spec-builder 消费，spec-lint 执法）：
  ① `node .zcode/zbase.mjs spec-lint` 逐条读 REQ-/NFR- 判定块（id 后 14 行窗口）：
     - 规范性关键词（SHALL/MUST/必须/不得/应当）缺失 = error——不设义务的需求无法验证；
     - REQ- 无触发词（WHEN/WHILE/IF/WHERE/当/若/一旦）= warning——测试用例不明显；
     - NFR- 无度量（数字+单位）= error——不可度量的质量需求门不了；
     - 验收锚（验收/Acceptance/Given/Verification/验证）缺失 = error——「完成」只是观点；
     - 占位词与模糊词以 lib/scan.mjs 的 SPEC_PLACEHOLDERS/SPEC_AMBIGUOUS 常量为准（本行刻意
       不罗列字面量——罗列即自击：lint 按行扫不识别注释，写进注释照样命中）；未知的东西
       写进⑥开放问题清单（红卡），不当场消化；
     - id 永不复用（append-only 资产，重号 = error）。
  ② `## 业务上下文` 标题（## 级、原样此五字）必须存在——spec-lint 检查存在性（SPEC_NO_BUSINESS_CONTEXT），
     不检查内容质量（四要素是否写实是签字闸的事，不机器化）。
  ③ FISU 标注（见业务上下文章节头部约定）：正文里凡非用户确认的判断，句首标【推断】并列依据；
     【建议】可被拒绝；无人能答的进⑥红卡区。签字前【推断】条目须逐条点名确认。
  ④ 章节序就是消费序：下游（架构/计划/派单）从①摘业务锚点，派单信封 Business 字段从①摘 2-3 句。
-->

## 业务上下文

<!--
  FISU 约定（全文档通用，本节先行声明）：
  【事实】= 用户确认过的原话转述（可追溯到对话或用户提供的材料）；
  【推断】= AI 补全、待确认（必须列出依据：观察到的代码/日志/用户哪句话）；
  【建议】= AI 的专业建议，用户可整体拒绝而不影响需求成立；
  未知 → 不许消化成【推断】，落⑥开放问题清单（红卡）。
  四要素（为什么做/谁真正受益/现实中怎么运转/例外与隐性规则）写实了，下游每一跳才有业务锚点——
  这里空着，后面全链都在猜。
-->

### 为什么做

<动机/触发事件：什么事让【现在】要做这个——不是「它有用」，是「什么变了/痛够了」。【事实】/【推断】标注>

### 谁真正受益

<角色 + 受益方式：谁的手上变轻松、变多轻松。不是「用户」，是具体角色在具体环节省了什么>

### 现实中怎么运转

<流程/环境/谁在什么情境用：这件事今天没有本产品时怎么干、在什么环境（系统/时段/频率）干>

### 例外与隐性规则

<什么时候不该这样/出过什么事：老用户口中的「一般是 X 但 Y 情况除外」、上次踩的坑——往往是最重要的需求>

### 术语表

| 术语 | 含义（本项目语境） |
|---|---|
| <术语> | <一句话定义；跨角色对齐用，避免同名异义> |

## 用户故事与场景

- 作为 <角色>，我在 <情境> 下做 <事>，是为了 <目的>。
- **反例（不做什么）**：<明确不覆盖的场景/角色——写在这里的不会被下游当成遗漏>

## 功能需求

<!--
  每条 REQ 必须可验收（能写出「跑什么命令/做什么操作算过」）且**至少挂一个具体 example**
  （example mapping：真实输入→预期输出，或操作序列）。问不出来的 example 落⑥红卡区，
  不许编造。全红 = 需求不成熟，停下报告，不硬写。
-->

| 编号 | 需求 | 验收 | Example（真实输入→预期输出/操作序列） |
|---|---|---|---|
| REQ-1 | 当 <触发条件> 时必须 <行为> | 验收：<命令/操作 + 期望输出> | <输入 X → 预期输出/操作序列> |

## 非功能需求

<八属性初步档位建议（critical/high/medium/low/minimal/none）——定档在 DFX 阶段细化，这里只给初步建议与理由；NFR- 条目须带度量（数字+单位）>

## 边界与依赖

- Out of Scope：<明确不做的>
- 依赖：<既有的系统/团队/外部契约>

## 开放问题清单（红卡区）

<!--
  红卡语义（example mapping）：无人能答的问题显式落盘，不许当场消化成假设。
  每条注明：问谁、阻塞哪些 REQ、当前假设（如有）。全红 = 需求不成熟，停下报告。
  禁止 AI 替答——列出来让用户拍板；拍板后条目移入正文并升级为【事实】。
-->

| 编号 | 问题 | 问谁 | 阻塞 | 当前假设 |
|---|---|---|---|---|
| OQ-1 | <问题> | <谁> | <REQ-N / 无> | <有则列，注明【推断】> |

## 签字

<签字人> ｜ <日期>

<!--
  签字语义：本签字覆盖两者——①本文档当前版本；②签字前 AI 的情境复述
  （「我理解：〔角色〕在〔情境〕下做〔事〕是为了〔目的〕；我假设：…；请纠偏」）。
  未复述的签字只是版本批——文档对了、理解错了照样做歪。内容任何变更须重新请批
  并重走复述（rules/workflow.md 签字闸）。
-->
